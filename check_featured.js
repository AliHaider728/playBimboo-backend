const mongoose = require('mongoose'); require('dotenv').config(); mongoose.connect(process.env.MONGODB_URI).then(async () => { const Product = require('./dist/models/Product').default || require('./dist/models/Product').Product; const count = await Product.countDocuments({ '': [{ isFeatured: true }, { isBestseller: true }] }); console.log('Featured count:', count); process.exit(0); }).catch(console.error);
